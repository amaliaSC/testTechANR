<form method="post" action="{{ path('app_rue_controller_r_delete', {'id': rue.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rue.id) }}">
    <button class="btn">Delete</button>
</form>
