{% extends 'base.html.twig' %}

{% block title %}Rue{% endblock %}

{% block body %}
    <h1>Rue</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rue.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ rue.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rue_controller_r_index') }}">back to list</a>

    <a href="{{ path('app_rue_controller_r_edit', {'id': rue.id}) }}">edit</a>

    {{ include('rue_controller_r/_delete_form.html.twig') }}
{% endblock %}
