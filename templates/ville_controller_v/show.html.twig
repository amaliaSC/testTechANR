{% extends 'base.html.twig' %}

{% block title %}Ville{% endblock %}

{% block body %}
    <h1>Ville</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ville.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ville.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ville_controller_v_index') }}">back to list</a>

    <a href="{{ path('app_ville_controller_v_edit', {'id': ville.id}) }}">edit</a>

    {{ include('ville_controller_v/_delete_form.html.twig') }}
{% endblock %}
